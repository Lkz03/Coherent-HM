// This is a good practice to explicitly set accessibility modifiers
// (public, private , etc) for classes and methods
// However, as we have not touched them yet, I am just note it once
public class Program
{
    //change the X in "TaskX.executeTask()" accordingly to the task in need.
    //For example: "TaskOne.executeTask()" to "TaskTwo.executeTask()".
    static void Main()
    {
        TaskTwo.ExecuteTask();
    }
}